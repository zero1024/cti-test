1. требования к xml (в методе приема данных об использовании печатных устройств) не строгие. Структура xml
проверяется на наличие обязательных полей, но лишние поля допустимы. Т.к. в задании требований к валидации xml
не было, сделал как посчитал нужным.
2. логирование не настраивал, посчитал это лишним для тестового задания.
3. для сборки и запуска приложения нужна Java8 и Maven (соотвественно в PATH переменной пути к ним тоже должны быть указаны)
4. файл базы данных (cti_print_db.mv.db) хранится в папке пользователя